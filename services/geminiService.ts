
// Serviço desativado conforme solicitação.
// Versículos e cálculos de monitoramento financeiro removidos.

export const getFinancialPrediction = async (): Promise<null> => {
  return null;
};

export const getDailyVerse = async (): Promise<null> => {
  return null;
};
